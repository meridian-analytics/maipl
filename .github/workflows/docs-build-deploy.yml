name: Build and Deploy Docs

on:
  push:
    branches:
      - main
    paths:
      - "docs/**"
      - ".github/workflows/docs-build-deploy.yml"
  pull_request:
    branches:
      - main
    paths:
      - "docs/**"

jobs:
  build:
    runs-on: self-hosted
    env:
      REGISTRY_URL: registry.maipl-dev.com
      REGISTRY_USERNAME: ${{ secrets.REGISTRY_USERNAME }}
      REGISTRY_PASSWORD: ${{ secrets.REGISTRY_PASSWORD }}
      DOCS_HOST: 192.168.239.166

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "18"
          cache: "npm"
          cache-dependency-path: docs/package-lock.json

      - name: Install dependencies
        working-directory: docs
        run: npm ci

      - name: Build documentation
        working-directory: docs
        run: npm run build

      # Only run registry and deployment steps on push to main
      - name: Login to registry
        if: github.event_name == 'push'
        run: |
          echo $REGISTRY_PASSWORD | docker login $REGISTRY_URL --username $REGISTRY_USERNAME --password-stdin

      - name: Build and push Docker image
        if: github.event_name == 'push'
        working-directory: docs
        run: |
          docker build -t $REGISTRY_URL/maipl-backend/docs-site:latest .
          docker push $REGISTRY_URL/maipl-backend/docs-site:latest

      - name: Deploy docs site
        if: github.event_name == 'push'
        run: |
          ssh ubuntu@$DOCS_HOST "\
            echo \"$REGISTRY_PASSWORD\" | sudo docker login $REGISTRY_URL --username $REGISTRY_USERNAME --password-stdin && \
            sudo docker pull $REGISTRY_URL/maipl-backend/docs-site:latest && \
            sudo docker stop docs-site || true && \
            sudo docker rm docs-site || true && \
            sudo docker run -d --name docs-site -p 3000:80 --restart unless-stopped $REGISTRY_URL/maipl-backend/docs-site:latest && \
            sudo docker system prune -f"
