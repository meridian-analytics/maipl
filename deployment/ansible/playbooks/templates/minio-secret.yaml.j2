apiVersion: v1
kind: Secret
metadata:
  name: minio-creds-kcmgkt2g55
  namespace: minio-system
type: Opaque
data:
  MINIO_ROOT_USER: "{{ minio_root_user | b64encode }}"
  MINIO_ROOT_PASSWORD: "{{ minio_root_password | b64encode }}"
  accesskey: "{{ minio_access_key | b64encode }}"
  secretkey: "{{ minio_secret_key | b64encode }}" 