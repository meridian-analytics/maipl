# <a name="top"></a> @maipl/buffer

```ts
function base64urlencode(bytes: Uint8Array): string
function hexDigest(bytes: ArrayBuffer): string
function sha256(bytes: ArrayBuffer): Promise<ArrayBuffer>
```
