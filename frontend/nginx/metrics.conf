server {
        listen       3200;
        server_name  test-metrics.maipl-dev.com;

        location / {
            root   /usr/share/nginx/html/metrics;
            index  index.html;
            try_files $uri $uri/ /index.html;
        }

        location ~ \.js$ {
            root /usr/share/nginx/html/metrics;
            add_header Content-Type application/javascript;
        }

        error_page   500 502 503 504  /50x.html;
        location = /50x.html {
            root   /usr/share/nginx/html/metrics;
        }
    }