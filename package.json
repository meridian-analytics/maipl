{
  "name": "@maipl/monorepo",
  "private": true,
  "scripts": {
    "@check": "biome check --apply .",
    "@format": "biome format --write .",
    "@build": "tsc && pnpm --parallel -F \"./apps/*\" build",
    "@preview": "docker compose up --build",
    "@start": "pnpm --parallel -F \"./apps/*\" start",
    "@tsc": "tsc"
  },
  "devDependencies": {
    "@biomejs/biome": "1.5.1",
    "@types/node": "^20.9.3",
    "typescript": "^5.3.2"
  }
}
